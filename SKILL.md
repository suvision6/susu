---
name: su-fenjingskill
description: 将完整剧本、未编号剧本段落，或边界明确的连续场景片段转换为生产可执行的六列导演分镜和 shot-data/2.4.3 结构化交付。用于锁源、整场导演规划、Beat 与事实、导演风格确认、保护完整视听过程、比较切与不切、设计真实剪辑点、对白观看权、摄影机、调度、表演、时长和必要连续性；不用于视频 Prompt、模型配置或合镜分组。
---

# 导演分镜

## 职责与边界

- 当前 Skill 与正式合同版本统一为 `2.4.3`。
- 接受完整剧本、未编号剧本段落，以及用户明确提交或锁定边界的连续台词／场景片段；不要求外部场号、剧本编号或既有 `scene_id`。
- 只有梗概、零散设想或无法确定连续原文边界时，拒绝正式拆镜；用户明确把该文本本身锁定为待拆片段后才可继续。
- 把锁定文本视为内容权威：逐字保留对白，不改变剧情事实、因果、人物关系、关键动作、道具状态与现实层。
- 允许与来源一致、不制造新剧情事实的可逆表演和调度选择；把它们留在导演字段，不得冒充 source fact。
- 只负责导演分镜；不生成视频 Prompt，不配置模型，不规划下游 Cut 链或多镜生成分组。

## 必读路由

进入对应阶段前，完整读取其唯一权威文件：

| 阶段 | 文件 | 唯一职责 |
| --- | --- | --- |
| 锁源与事实 | [source-and-beat.md](references/source-and-beat.md) | 输入边界、文本锁、source span、Beat、受保护事实与事实覆盖 |
| 导演风格 | [director-profile.md](references/director-profile.md) | Gate 1、导演倾向与候选比较 |
| 规划与拆镜 | [shot-design.md](references/shot-design.md) | 整场理解、完整过程、切与不切、Gate 2、摄影机、调度与时长 |
| 对白调度 | [dialogue-staging.md](references/dialogue-staging.md) | 发言权、画面所有权、声音位置与特殊观看策略 |
| 情绪与表演 | [performance-arc.md](references/performance-arc.md) | 可见表演、阶段分析与跨镜继承 |
| 连续性 | [continuity-contract.md](references/continuity-contract.md) | 必要状态追踪、轴线、方向与动作接续 |
| 数据与交付 | [output-contract.md](references/output-contract.md) | shot-data/2.4.3、两 Gate digest、最小镜头结构、六列与报告 |

不要在其他文件重定义 owner 细则。

## 工作流

### 1. 锁源与 Gate 1

1. 锁定全文、批准修正、边界与 hash；缺少外部编号时按来源顺序生成稳定内部 ID。
2. 展示源边界、不可改写约束和本轮导演倾向；用户未指定风格时才提供至少两个可比较选项。
3. 要求用户明确确认风格，把实际展示的 Gate 1 材料绑定到 digest，然后停止。

### 2. 整场规划与 Gate 2

1. Gate 1 后建立 Beat 与受保护 facts；情绪、对白和连续性分析只在场景需要时展开。
2. 先理解整场的目标、推进、视点和节奏，再找出不应被切断的动作、表演、声音与空间过程。
3. 比较继续不切能保留什么、现在切开能增加什么，再形成最少且足够的镜头单元。
4. 每个真实剪辑点写明具体触发和相对于不切的视听收益；禁止按原文行、标点、台词句数、人物数或景别轮换反推镜头数。
5. 展示场级策略、镜头单元、每刀触发与收益、必要风险、镜头数和总时长；确认后绑定实际展示的 Gate 2 材料，然后停止。

### 3. 最终分镜与交付

1. Gate 2 后直接生成最终分镜，不设置第三个人工 Gate；改变镜头单元、顺序或剪切点时重新 Gate 2。
2. 先决定观众站在哪里看，再设计景别、机位、构图、人物调度、表演、声音、时长和镜头结束状态。
3. 对白发言权不等于画面所有权；倾听者、画外、遮挡、背面、共享构图、连续重构和延迟反打都可成立。
4. 每镜把环境、摄影机相对位置与朝向、可见画面、人物动作表情、逐字台词和动作后的可见状态融合为一段自然语言画面内容；真实环境变化和人物位移必须写清。
5. 第五列只显示统一摄影三要素和 `【画面内容】`，不单列机位构图、站位位移、人物表演、声音或镜头结束；备注由时间块生成时长估算，再按需追加第五列之外的执行提醒。
6. 读取 [output-contract.md](references/output-contract.md) 构建并独立校验四文件。JSON 是机器事实源；任何 FAIL 停止正式交付，WARN 只保留真实导演审计信号。

## 不可违反

- Beat、Fact、台词句数和人物数量都不等于镜头数。
- `director_analysis` 不得进入 source facts 或对白。
- 每刀必须有具体触发和剪辑收益，不能只写类别词或“换景别”。
- 剧情事实必须由镜头覆盖；覆盖关系不要求逐字段重复举证。
- 已追踪连续性必须继承；有意越轴或断裂必须明确声明。
- 两个 Gate 只记录当前阶段的明确确认，不认证用户身份；受绑定内容变化时旧 digest 失效。
